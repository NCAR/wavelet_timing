if(tag != '') tag = paste0('_',tag)
figure_name = paste0(
  fig_supp_table,'_',figure_number,'_',
  location,'_',time_period,tag,'.png')

cat('\n')
cat('-----------------------------------------------------------------------------\n')
cat(figure_name)
cat('\n\n')
